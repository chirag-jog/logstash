input {
  lumberjack {
    port => 5000
    type => "logs"
    ssl_certificate => "<%= scope.lookupvar('logstash::tlskeys::tls_cert') %>"
    ssl_key => "<%= scope.lookupvar('logstash::tlskeys::tls_key') %>"
  }
}
