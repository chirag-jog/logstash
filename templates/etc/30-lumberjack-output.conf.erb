output {
  elasticsearch_river {
    es_host => "<%= @es_host %>"
    rabbitmq_host => "<%= @rabbitmq_host %>"
    user => "<%= @rabbitmq_user %>"
    password => "<%= @rabbitmq_password %>"
    vhost => "<%= @rabbitmq_vhost %>"

  }
  stdout { codec => rubydebug }
}
